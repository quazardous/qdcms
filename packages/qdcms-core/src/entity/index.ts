/**
 * @quazardous/qdcore/entity — public exports.
 *
 * See `./types.ts` for the doc and rationale on each contract. This file
 * exists only to keep the import surface tidy.
 */

export type {
  EntityFieldType,
  EntityFieldConfig,
  EntityIndexConfig,
  EntityDescriptor,
  EntityRegistry,
  Query,
  Repository,
  BackendStorage,
  FrontendStorage,
} from './types'
